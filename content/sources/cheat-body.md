The full "Claude Code, Locked Down" guide explains the why. This is the one-pager for after you've read it — every permission mode, rule pattern, settings file, and lockdown flag in a single reference you can keep open (or printed) next to your terminal.

## On the page

- The four permission modes, what each one auto-approves, and when it's the right choice
- Rule syntax at a glance — command prefixes, gitignore-style path blocks, and why deny always beats allow
- The three settings files and which one wins when they disagree
- A copy-paste deny-first permissions block that protects **.env** files, SSH keys, and cloud credentials
- Scope and session controls — workspace rules, granting extra folders deliberately, and the flag that should never leave a disposable container

One page, no fluff. Print it, pin it, skip re-reading the docs.
